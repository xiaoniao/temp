com.kunyao.assistant
    |
	|
	|-------- core
	|         |
	|         |
	|         |-------- constant  // 常量层：用于定义及存放常亮
	|         |
	|         |-------- dto       // 数据传输对象层：与客户端通信时定义的数据模型对象
	|         |
	|         |-------- entity    // 自定义实体层：如分页实体等涉及到业务且需要封装的类
	|         |
	|         |-------- enums     // 枚举层：常用枚举定义，如一些常用状态
	|         |
	|         |-------- exception // 自定义异常层：用于定义些项目中的自定义异常
	|         |
	|         |-------- feature   // 功能层：用于封装实现些第三方或自己封装的功能代码，如cache，短信，SQL生成等。
	|         |
	|         |-------- generic   // 通用层：用于封装实现些通用方法，如数据库CURD
	|         |
	|         |-------- model     // 模型层：与数据库相对应的数据模型层
	|         |
	|         |-------- utils     // 工具层
	|
	|
	|-------- web
	          |
	          |
	          |-------- controller // 控制层，存放springMvc控制器
	          |
	          |-------- dao        // 数据访问层，用于实现数据库方法
	          |
	          |-------- filter     // 拦截器层
	          |
	          |-------- init       // 初始化层
	          |
	          |-------- interceptors  // 过滤器层
	          | 
	          |-------- log        // 日志层 AOP实现
	          |
	          |-------- security   // 安全层 shiro
	          |
	          |-------- service    // 业务层，用于实现业务代码
	